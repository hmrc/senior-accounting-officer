info:
  name: Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/test-only/work-item/s1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
