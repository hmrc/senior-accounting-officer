info:
  name: (400) no-correlation-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/subscription"
  headers:
    - name: Authorization
      value: "{{authorization}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
