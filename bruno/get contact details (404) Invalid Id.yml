info:
  name: get contact details (404) Invalid Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/contact-details/456"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
