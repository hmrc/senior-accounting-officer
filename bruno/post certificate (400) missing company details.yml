info:
  name: post certificate (400) missing company details
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/certificate"
  body:
    type: json
    data: |-
      {
        "subscriptionId": " ",
        "submitterName": "Subname",
        "saoName": "SubmitterTestName",
        "saoEmail": "test@email.com",
        "companies": []
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
