info:
  name: get contact details (200)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/contact-details/123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
