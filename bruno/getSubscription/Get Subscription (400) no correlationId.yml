info:
  name: Get Subscription (400) no correlationId
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/subscription"
  headers:
    - name: Authorization
      value: "{{authorization}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
