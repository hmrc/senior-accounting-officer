info:
  name: get obligation (404) Invalid Id
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/obligation/456"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
