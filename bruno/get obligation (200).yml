info:
  name: get obligation (200)
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/obligation/123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
